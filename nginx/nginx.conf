server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://frontend:80;
    }
    location /api/ {
        proxy_pass http://backend:5080/;
    }
    location /health/ {
        proxy_pass http://backend:5080/health;
    }
}
